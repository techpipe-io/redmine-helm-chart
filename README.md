# Redmine Helm repository

This repository contains a production-ready Helm chart for Redmine with optional Gateway API HTTPRoute support.

The chart itself lives in `charts/redmine`, refer to its [README.md](charts/redmine/README.md) for details. 
